model Verification {
    id        String @id @default(uuid())

    email     String? @unique
    phone     String? @unique

    codeHash  String
    expiresAt DateTime
    attempts  Int      @default(0)
    createdAt DateTime @default(now())

    @@map("verifications")
}
